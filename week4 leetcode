int timeRequiredToBuy(int* tickets, int n, int k) {
    int count=0;
    while(1){
        for(int i=0;i<n;i++){
            if(tickets[i]==0) continue;
            tickets[i]-=1;
            count+=1;
            if(tickets[k]==0) return count;
        }
    }
    

}
